[project]
name = "url-shortener"
version = "0.1.0"
description = "User Shortener"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.116.1",
    "redis[hiredis]>=6.4.0",
    "typer>=0.16.0",
    "uvicorn>=0.35.0",
]
